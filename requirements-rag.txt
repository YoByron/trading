# Optional RAG System Dependencies
# Only install if using vector database and embeddings
# WARNING: These add ~600MB and 4-6 minutes to install time

# Install with: pip install -r requirements-minimal.txt -r requirements-rag.txt

# Vector Database + Embeddings
chromadb==0.6.3
sentence-transformers==3.4.1

# Note: sentence-transformers pulls in:
# - torch (~500MB)
# - transformers (~200MB)
# - huggingface-hub (~50MB)
# Total: ~750MB additional dependencies
