# ACT Configuration for Local GitHub Actions Testing
# See: https://github.com/nektos/act

# Use larger Ubuntu image for better compatibility
-P ubuntu-latest=catthehacker/ubuntu:act-latest

# Use macOS image for macOS workflows (limited support)
-P macos-latest=catthehacker/macos:act-latest

# Container platform (auto-detect)
--container-architecture linux/amd64

# Reuse containers for faster runs
--reuse

# Verbose output for debugging
-v
